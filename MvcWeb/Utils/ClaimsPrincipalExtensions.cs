//using MvcWeb.Models.Auth;

using MvcWeb;

using System.Security.Claims;

namespace System {
  public static class ClaimsPrincipalExtensions {
    //public static UserContext GetUserContext(this ClaimsPrincipal principal) =>
    //    principal.Identity.IsAuthenticated
    //    ? new() {
    //      UserName = principal.UserName(),
    //      Email = principal.Email(),
    //      DisplayName = principal.FindFirstValue(Constants.Auth.Claims.DisplayName),
    //      FirstName = principal.FindFirstValue(Constants.Auth.Claims.FirstName),
    //      LastName = principal.FindFirstValue(Constants.Auth.Claims.LastName),
    //    }
    //    : null;

    //public static string UserName(this ClaimsPrincipal principal) => principal.FindFirstValue(Constants.Auth.Claims.UserName);
    //public static string DisplayName(this ClaimsPrincipal principal) => principal.FindFirstValue(Constants.Auth.Claims.DisplayName);
    //public static string Email(this ClaimsPrincipal principal) => principal.FindFirstValue(Constants.Auth.Claims.Email);
    //public static string FirstName(this ClaimsPrincipal principal) => principal.FindFirstValue(Constants.Auth.Claims.FirstName);
    //public static string LastName(this ClaimsPrincipal principal) => principal.FindFirstValue(Constants.Auth.Claims.LastName);
   }
}
